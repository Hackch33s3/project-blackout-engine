services:
  engine:
    build: .
    container_name: project-blackout-engine
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ENGINE_URL=http://engine:8000
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://engine:8000')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          memory: 1536M

  worker:
    build: .
    container_name: project-blackout-worker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - ENGINE_URL=http://engine:8000
      - PYTHONUNBUFFERED=1
    command: python -u queue_worker.py
    depends_on:
      - engine
